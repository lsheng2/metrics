## 1. OpenSpec Contract

- [x] 1.1 Add dashboard UI design-system requirements to `dashboard-ui-baseline`.

## 2. Shared UI Components

- [x] 2.1 Define shared CSS classes for edit forms, form grids, form fields, action bars, action groups, dirty fields, required tags and status tags.
- [x] 2.2 Split tag typography from button/input typography so badges do not render like controls.
- [x] 2.3 Keep compatibility aliases for existing provider/scope selectors.

## 3. Template Migration

- [x] 3.1 Migrate Provider Profile Config to shared design-system classes.
- [x] 3.2 Migrate Bug Trend Scope Config to shared design-system classes.

## 4. Validation

- [x] 4.1 Add static UI design-system drift tests.
- [x] 4.2 Add/extend browser tests for Provider Profile and Scope Config button/font/dirty behavior.
- [x] 4.3 Run focused UI tests, related regression tests, Django check, migration dry-run, OpenSpec strict validation and live browser smoke.
