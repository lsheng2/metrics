---
name: beautiful-mermaid
description: Render, validate, and harden Mermaid diagrams as SVG and PNG using the Beautiful Mermaid library. Use when the user asks to render a Mermaid diagram or needs Mermaid syntax made compatible with VS Code Markdown preview.
---

# Beautiful Mermaid Adapter

Canonical source for this skill is the user-level Copilot skill:

`%USERPROFILE%/.copilot/skills/beautiful-mermaid/SKILL.md`

Load that file first and follow its referenced resources. This project file exists only to expose the same skill through the repository-level Copilot skill surface.

If the canonical source is missing, report that the shared `beautiful-mermaid` skill is not installed locally before attempting diagram rendering.
